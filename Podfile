# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Alumni Connect' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Alumni Connect
  pod 'Firebase/Core'
  target 'Alumni ConnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Alumni ConnectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
